feat(auth): configure JWT module with async factory

Set up JWT authentication with environment-based configuration:
- Imported JwtModule in AuthModule with registerAsync
- Used ConfigService to read JWT_SECRET and JWT_EXPIRATION from .env
- Configured signOptions with 7-day token expiration
- Added fallback defaults for development safety
- Exported AuthService for use in other modules

JwtService now available for token generation and verification.
Verified with successful npm run build (all workspaces).