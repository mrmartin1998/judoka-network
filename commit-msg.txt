feat(prisma): set up Prisma service for database access

Create Prisma service to manage database connections:
- Created prisma.service.ts extending PrismaClient
- Implemented onModuleInit for automatic connection
- Implemented onModuleDestroy for graceful shutdown
- Created prisma.module.ts as Global module
- Imported PrismaModule in AppModule

PrismaService now available for dependency injection across entire app.
Verified with successful npm run build (all workspaces).