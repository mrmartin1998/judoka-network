feat(auth): configure Google OAuth strategy

Implement Passport Google OAuth 2.0 strategy for authentication:
- Created google.strategy.ts extending PassportStrategy
- Configured Google OAuth with clientID, clientSecret, and callbackURL
- Implemented validate() method to extract user profile data
- Extracts email, firstName, lastName, and picture from Google profile
- Registered GoogleStrategy as provider in AuthModule
- Strategy handles OAuth flow and returns formatted user data

Google OAuth strategy ready - enables "Login with Google" flow.
Task 1.6 complete. Section C (Google OAuth Implementation) in progress.
Verified with successful npm run build (backend workspace).