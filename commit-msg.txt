feat(auth): create JWT strategy for token validation

Implement Passport JWT strategy for authentication:
- Created jwt.strategy.ts extending PassportStrategy
- Configured JWT extraction from Bearer token in Authorization header
- Implemented validate() method to look up users from token payload
- Integrated PrismaService for database user lookup
- Registered JwtStrategy as provider in AuthModule
- Imported PassportModule for Passport infrastructure
- Installed passport-jwt and @types/passport-jwt packages

Strategy validates JWT tokens and attaches user to request object.
Verified with successful npm run build (all workspaces).